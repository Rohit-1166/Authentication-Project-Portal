{
  "name": "authentication-project-root",
  "version": "1.0.0",
  "description": "Root runner for authentication project portal",
  "scripts": {
    "start": "concurrently \"npm run start --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "dependencies": {
    "adm-zip": "^0.5.17",
    "concurrently": "^8.2.2"
  }
}
